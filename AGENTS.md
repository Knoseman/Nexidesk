# Nexidesk

## Cursor Cloud specific instructions

This repository is currently empty (initial commit with only a `README.md`). There are no services, dependencies, or build steps to run.

When code is added to this repository, update this section with:
- How to install dependencies (package manager and commands)
- How to run the development server(s)
- How to run lint and tests
- Any non-obvious environment setup (e.g. required secrets, database setup, etc.)
